$kind: http-request
url: "{{baseUrl}}/api/admin/orders"
method: GET
scripts:
  - type: afterResponse
    code: |-
      const body = pm.response.json();
      if (body.orders?.length) pm.collectionVariables.set('adminOrderId', body.orders[0].id);
order: 18000
