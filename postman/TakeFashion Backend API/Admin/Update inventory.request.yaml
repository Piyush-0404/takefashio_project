$kind: http-request
url: "{{baseUrl}}/api/admin/inventory/{{adminVariantId}}"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "quantity": 25,
      "reservedQuantity": 2
    }
order: 8000
