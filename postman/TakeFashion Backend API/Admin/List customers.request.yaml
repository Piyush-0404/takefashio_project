$kind: http-request
url: "{{baseUrl}}/api/admin/users"
method: GET
scripts:
  - type: afterResponse
    code: |-
      const body = pm.response.json();
      if (body.users?.length) pm.collectionVariables.set('adminUserId', body.users[0].id);
order: 21000
