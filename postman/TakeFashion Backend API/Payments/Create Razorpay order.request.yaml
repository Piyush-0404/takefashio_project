$kind: http-request
url: "{{baseUrl}}/api/payments/create-order"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "orderId": "{{orderId}}"
    }
order: 1000
