$kind: http-request
url: "{{baseUrl}}/api/admin/products"
method: GET
scripts:
  - type: afterResponse
    code: |-
      const body = pm.response.json();
      if (body.products?.[0]) pm.collectionVariables.set('productId', body.products[0].id);
    language: text/javascript
order: 3000
