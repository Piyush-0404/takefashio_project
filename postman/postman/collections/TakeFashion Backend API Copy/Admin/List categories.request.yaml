$kind: http-request
url: "{{baseUrl}}/api/admin/categories"
method: GET
scripts:
  - type: afterResponse
    code: |-
      const body = pm.response.json();
      if (body.categories?.[0]) pm.collectionVariables.set('categoryId', body.categories[0].id);
    language: text/javascript
order: 6000
